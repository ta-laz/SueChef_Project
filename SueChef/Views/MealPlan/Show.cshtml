<!-- VIEW FOR SINGLE MEAL PLANS -->

@model List<SueChef.ViewModels.RecipeCardViewModel>

<div class="justify-center">
    @* List of All Recipes in Meal Plan: *@
    <section class="mb-8">
        <div class="max-w-6xl mx-auto mb-2">
            <h2 class="inline text-2xl font-semibold text-gray-800 mb-4 trackling-tight">Meal Plan Recipes</h2>
        </div>

        @foreach (SueChef.ViewModels.RecipeCardViewModel recipe in Model)
        {
            <partial name="_MealPlanRecipeCardPartial" model="recipe" />
        }
    </section>
</div>